# Local agent instructions are specific to this working copy and must never be committed.
AGENTS.md

# .NET build output
bin/
obj/

# IDE / editor
.vs/
.vscode/
*.user

# OS junk
Thumbs.db
.DS_Store
# runtime artifacts written into the fake game by demos/tests
samples/fake-game/cardshopmodmanager.*

# credential material - never commit (key lives outside the repo via DPAPI)
nexus-key*
oauth-settings.json
nexus-oauth-tokens*
*.key
*api-key*
*apikey*
secrets.json

# publish output
/dist/
